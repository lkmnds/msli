import sys

argv = sys.argv

def msl_tokenize(s):
    i = 0
    tokens = []
    while i < len(s):
        char = s[i]
    return tokens

def main():
    if len(argv) > 1
        with open(argv[1], 'r') as f:
            p = tokenize(f.readAll())
            execute(p)

if __name__ == '__main__':
    main()
